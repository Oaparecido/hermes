{
  "type": "postgres",
  "host": "localhost",
  "port": "5432",
  "username": "hermes",
  "password": "hermes",
  "database": "hermes",
  "entities": ["dist/**/*.entity{.ts,.js}"],
  "synchronize": true,
  "migrations": ["migration/*.ts"],
  "cli": {
    "migrationsDir": "migration"
  }
}
